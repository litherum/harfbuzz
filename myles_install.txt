macOS:
cd build
CFLAGS="-O0 -g" CXXFLAGS="-O0 -g" cmake -DCMAKE_INSTALL_PREFIX=/Users/litherum/src/harfbuzz/install ..
CFLAGS="-O0 -g" CXXFLAGS="-O0 -g" cmake --build . --target install

Windows:
cmake -A x64 -DCMAKE_INSTALL_PREFIX="C:\Users\lithe\source\repos\harfbuzz\install" ..
cmake --build . --target install

